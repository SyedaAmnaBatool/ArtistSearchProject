describe("Artist Search", function() {
  it("checks if input value null or not", function() {
    expect(true).toBe(true);
  });


  it("checks if JSON data is null or not", function() {
    a = true;

    expect(a).toBe(true);
  });
});